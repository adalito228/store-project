// porxy que redireccioneproxy inverso 

// Reac angular esta etiqueta la llaman app (nosotros trabajmos con arquitectura client render server render es mas r
//   rapido pero se necesita un motor de renderizadoi en servidor, para eso sirve Next.js o Laravel, react client render)(http2 para 
//   hacer muchas llamadas )
/*Reac angular esta etiqueta la llaman app (nosotros trabajmos con arquitectura client render server render es mas r
  rapido pero se necesita un motor de renderizadoi en servidor, para eso sirve Next.js o Laravel, react client render)(http2 para 
  hacer muchas llamadas )*/

  //linter diferentes versiones, paras java también
  //web es forma de presentar datos
  //No toda api es una web, pero una web si es una api

  /* 
  cuando cmabia url, el componente pages, es un componente que e nagular se llama app 

  */